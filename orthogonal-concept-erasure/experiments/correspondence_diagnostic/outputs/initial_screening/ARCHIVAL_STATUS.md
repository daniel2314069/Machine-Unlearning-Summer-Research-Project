# Archival status

This directory is retained unchanged as the **preliminary vector-wise
screening (old concept set)**.

- The vector-wise results are an **Eq. 6 ablation**, not the official OCE
  baseline.
- The old N=5 blocked gate is **not applicable to the new
  official-subspace experiment**.
- The existing `cat -> dog` subspace smoke result remains an archival result.
  The new official-subspace run uses its own isolated output directory.

No old image, checkpoint, metric, or report was deleted.
